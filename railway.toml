# Railway configuration for NearMe
# This file tells Railway how to build and deploy each service.
# See: https://docs.railway.app/reference/config-as-code

[build]
builder = "DOCKERFILE"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
